<% if @errors  %>
  <ul>
    <% @errors.each do |error| %>
    <li>
      <%= error %>
    </li>
    <% end %>
  </ul>
<% end %>


<%= form_tag "/client/posts/#{@post["id"]}", method: :patch do %>
  <div>
    Title: <input type="text" name="title" value="<%= @post["title"] %>" />
  </div>
  <div>
    Content: <input type="text" name="content" value="<%= @post["content"] %>" />
  </div>
  <div>
    Latitude: <input type="text" name="latitude" value="<%= @post["latitude"] %>" />
  </div>
  <div>
    Longitude: <input type="longitude" name="longitude" value="<%= @post["longitude"] %>">
  </div>
  <input type="submit" name="" value="Update Post"/>
<% end %>

